//
//  Zodiac.swift
//  interactiveApp
//
//  Created by Scholar on 26/07/2024.
//

import SwiftUI

struct Zodiac: View {
    var body: some View {
        Text("ZODIAC QUIZ")
        Button(action: {
            
        }) {
            Text("Aries")
                .padding()
                .background(Color.blue)
                .foregroundColor(.white)
                .cornerRadius(10)
        }
        .padding(.bottom, 10)
        
        Button(action: {
            
        }) {
            Text("Taurus")
                .padding()
                .background(Color.blue)
                .foregroundColor(.white)
                .cornerRadius(10)
        }
        
        .padding(.bottom, 10)
        
        
        Button(action: {
            
        }) {
            Text("Gemini")
                .padding()
                .background(Color.blue)
                .foregroundColor(.white)
                .cornerRadius(10)
        }
        
        .padding(.bottom, 10)
        .padding(.bottom, 10)
        
        
        Button(action: {
            
        }) {
            Text("Cancer")
                .padding()
                .background(Color.blue)
                .foregroundColor(.white)
                .cornerRadius(10)
        }
        .padding(.bottom, 10)
        Button(action: {
        }) {
            Text("Leo")
                .padding()
                .background(Color.blue)
                .foregroundColor(.white)
                .cornerRadius(10)
        }
        .padding(.bottom, 10)
        
        Button(action: {
            
        }) {
            Text("Virgo")
                .padding()
                .background(Color.blue)
                .foregroundColor(.white)
                .cornerRadius(10)
        }
        
        .padding(.bottom, 10)
        
        
        Button(action: {
            
        }) {
            Text("Libra")
                .padding()
                .background(Color.blue)
                .foregroundColor(.white)
                .cornerRadius(10)
        }
        
        .padding(.bottom, 10)
        .padding(.bottom, 10)
        
        
        Button(action: {
            
        }) {
            Text("Scorpio")
                .padding()
                .background(Color.blue)
                .foregroundColor(.white)
                .cornerRadius(10)
        }
        Button(action: {
            
        }) {
            Text("Gemini")
                .padding()
                .background(Color.blue)
                .foregroundColor(.white)
                .cornerRadius(10)
        }
        
        .padding(.bottom, 10)
        .padding(.bottom, 10)
        
        
    }
    
}
#Preview {
    Zodiac()
}
